Warning: no stdin data received in 3s, proceeding without it. If piping from a slow command, redirect stdin explicitly: < /dev/null to skip, or wait longer.
Good — return type is annotated. Now let me do a final summary check on all new and modified files:

**Files created:**
- `src/domains/management/_shared/hooks/useWakeLock.ts` — extracts WakeLock effect
- `src/domains/management/_shared/hooks/useAlertAudio.ts` — extracts audio ref + 3 effects, returns `{ stopSound }`
- `src/domains/management/_shared/hooks/useAdminUIState.ts` — extracts 14 useState calls, returns typed `AdminUIState`
- `src/domains/management/orders/components/AdminPageModals.tsx` — extracts modal/popup render block

**Files modified:**
- `src/domains/management/orders/hooks/useAdminOrders.ts` — added `getAllActiveOrders` initial fetch + `loading` state to result
- `src/domains/management/orders/hooks/useAdminOrders.test.ts` — added `getAllActiveOrders` mock
- `src/shared/api/__mocks__/public.ts` — added `getAllActiveOrders` mock export
- `src/AdminHomePage.tsx` — now 118 lines, uses all new hooks/components

The two preexisting `as X` casts have been preserved with their comments:
- `String()` coercion comment → moved to `useAdminOrders.ts:71`
- `response as unknown as { orders: Order[] }` cast → moved to `useAdminOrders.ts:74`

Please run the verification commands now:

```sh
wc -l src/AdminHomePage.tsx
yarn tsc --noEmit
yarn test --watchAll=false
```

**Expected:**
- Line count: **118** ✓ (≤120)
- tsc: 0 errors
- All existing test suites pass (useAdminOrders tests updated to mock `getAllActiveOrders`)

The circular dependency between `useAlertAudio` (needs `alertOrder`/`editedOrder`) and `useAdminOrders` (needs `stopSound`) is resolved via an `audioStopRef` proxy in the shell — `stopSoundProxy` is passed to `useAdminOrders`, then after `useAlertAudio` runs each render, `audioStopRef.current` is patched to the real `stopSound`. WebSocket callbacks fire asynchronously so by the time they call `stopSoundRef.current()`, all renders are complete and the ref is wired correctly.
