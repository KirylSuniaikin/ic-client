// Web-kiosk timing constants.

/**
 * How long the customer gets to read the order-placed confirmation sheet before the kiosk resets
 * itself for the next customer.
 */
export const ORDER_PLACED_AUTO_RETURN_MS = 15_000;
